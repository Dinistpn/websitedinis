{% extends "prelogin-layout.html" %}

{% block title %}
    Home
{% endblock %}

{% block content %}
    <h3>Welcome {{username}} you can make your search here!</h3>
    
    <p> &nbsp </br>
        In the painel under you can see the most active user, the last user to have 
        commented/reviewed one the pictures, finally you can find the landscape and weather 
        with more comments/reviews.</br>
        Please search for pictures in the search bar and give you feedback, your feedback is 
        much apreciated!
        </br>&nbsp
    </p>
    
    <div class="results-container" style=" width: 65rem; height: 12rem; border-style: inset; 
    border-color:blue;">
            
        <div class="card2">            
            <div class="card-body2">   
                <h6>Most Recent contributor: <h6 class="ee">
                {{recent[0]['username']}}</h6>
                <h6>__________________________</h6>
                <h6>__________________________</h6>
            </div>
        </div> 
        <div class="card2">
            <div class="card-body2">
                <h6>Most popular landscape: <h6 class="ee">
                {{landscape[0]['landscape']}}</h6> </h6>
                <h6>No comments: <h6 class="ee">
                {{landscape[0]['comments']}}</h6></h6>
                <h6>No ratings:<h6 class="ee"> 
                {{landscape[0]['ratings']}}</h6></h6>
            </div>
        </div>    
        <div class="card2">
            <div class="card-body2">
                <h6>The most active user:<h6 class="ee"> 
                {{maxP[0]['username']}}</h6></h6>
                <h6>No comments:<h6 class="ee"> 
                {{maxP[0]['comments']}}</h6></h6>
                <h6>No ratings:<h6 class="ee"> 
                {{maxP[0]['rating']}}</h6></h6>
            </div>
        </div>   
        <div class="card2">
            <div class="card-body2">    
                <h6>Most popular weather: <h6 class="ee"> 
                {{weather[0]['weather']}}</h6></h6>
                <h6>No comments: <h6 class="ee"> 
                {{weather[0]['comments']}}</h6></h6>
                <h6>No ratings: <h6 class="ee"> 
                {{weather[0]['ratings']}}</h6></h6>
            </div>
        </div>    
    </div>
    <form action="/results" method="GET" style="padding-top:3rem;">
        <div class="form-group">
            <input type="text" class="form-control" name="image" autofocus aria-describedby="bookHelp" placeholder="Idphoto or Name">
            <small id="bookHelp" class="form-text text-muted">Choose a name of id/location, example: 1, Krakow, rain, lagoon</small>
        </div>
        <button class="btn btn-primary" type="submit">Search</button>
    </form>
{% endblock %}
